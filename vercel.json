{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"installCommand": "alias bun=\"~/.bun/bin/bun\" && curl -fsSL https://bun.sh/install | bash && bun install --frozen-lockfile",
	"devCommand": "bun dev",
	"buildCommand": "bun vercel:build",
	"framework": "sveltekit",
	"outputDirectory": ".svelte-kit/output"
}